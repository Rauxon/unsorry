import Mathlib

theorem face_count_unique_from_linear_identity (p q F F' : ℕ) (hq : 0 < q) (hF : (p * q) * F + 4 * q = (2 * p + 2 * q) * F) (hF' : (p * q) * F' + 4 * q = (2 * p + 2 * q) * F') : F = F' := by
  sorry
