import Mathlib.Algebra.Group.Basic

/-- Goal `nat-add-comm` (backlog: "Addition of natural numbers is commutative").
Canonical statement `∀x₁,x₂∈ℕ:x₁+x₂=x₂+x₁`. -/
theorem nat_add_comm : ∀ a b : ℕ, a + b = b + a := by
  sorry
